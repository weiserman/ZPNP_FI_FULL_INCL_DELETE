projection;
strict ( 2 );
use draft;

define behavior for ZAP_C_VAL_UI //alias <alias_name>
use etag
{
  use create;
  use update;
//  use delete;

  use action Activate;
  use action Discard;
  use action Edit;
  use action Resume;
  use action Prepare;

  use action validateData;

  use association _Items { create; with draft; }
  use association _Logs { create; with draft; }
}

define behavior for ZAP_C_VAL_ITEM_UI //alias <alias_name>
use etag
{
  use update;
//  use delete;

  use association _Val { with draft; }
}

define behavior for ZAP_C_VAL_LOG_UI //alias <alias_name>
{
//  use update;
//  use delete;

  use association _Val { with draft; }
}